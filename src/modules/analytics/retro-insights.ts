export async function generateRetroInsights(sprintId: string) {
    // Placeholder for GenAI-powered retrospective insights
    // Summarizes action items and connects past recurring issues
    return {
        sprintId,
        keyThemes: ['Communication issues with QA', 'Deployment bottleneck'],
        suggestedActions: [
            'Automate staging deployment pipeline',
            'Include QA in daily standups'
        ]
    };
}
